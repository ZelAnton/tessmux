//! Example integration test.
//!
//! Files in `tests/` are each compiled as a separate crate and run with
//! `cargo test`. Replace this with tests that exercise your crate's public
//! surface (for a library) or shell out to the built binary (for a CLI).

#[test]
fn placeholder_integration() {
    let sum = 2 + 2;
    assert_eq!(sum, 4);
}
